<p style="color: green"><%= notice %></p>

<%= render @user %>

<div>
  <% if @current_user.name == @user.name %>
    <%= link_to 'Edit', edit_user_path(@user)%> <br>
    <%= link_to "Destroy this user", user_path(@user), method: :delete%> <br>
  <% end %>

  <%= button_to "Destroy this user", @user, method: :delete %>
</div>
